# Implementation Notes — Issue #121 フィード横断新着一覧機能

## Implementation Notes

### Task 1

- 採用方針: `user_cross_feed_views` テーブル（UUID PK + last_seen_at + updated_at）と対応する Go ドメインモデル `UserCrossFeedView` を新規追加。
- 重要な判断: design.md / tasks.md が指定する migration timestamp `20260528120000` が既存 `20260528120000_add_item_search_indexes` と完全衝突するため、同日 1 時間後ろ倒しの `20260528130000` に micro-adjust した（採番のみの差異で意味的変更なし）。
- 残存課題: なし。

### Task 2

- 採用方針: `UserCrossFeedViewRepository` interface を `internal/repository/interfaces.go` に追加し、`internal/repository/postgres_user_cross_feed_view_repo.go` に Postgres 実装（`Get` / `Upsert`）を新設。Upsert は `INSERT ... ON CONFLICT (user_id) DO UPDATE SET last_seen_at=EXCLUDED.last_seen_at, updated_at=now()` で冪等性を確保し、`updated_at` は DB 側 `now()` で統一して時刻ドリフトを避ける。
- 重要な判断: integration test の DB セットアップは既存 `setupItemSearchTestDB` / `setupListDueTestDB` と同パターン（`TEST_DATABASE_URL` 環境変数 fallback + Skip ガード + 全テーブル DROP→`database.RunMigrations`）を採用し、ヘルパ名は他テスト群と衝突しないよう `setupCrossFeedViewTestDB` / `insertTestUserForCrossFeedView` / `crossFeedViewTestDatabaseURL` と命名で分離した（既存 `insertTestItem` 重複事象と同種の衝突を予防）。`cleanupSQL` は `user_cross_feed_views` を先頭に追加（CASCADE で消えるが明示的に列挙）。
- 残存課題: なし。Task 1 で確認済の `postgres_item_repo_starred_test.go` の `insertTestItem` 重複事象は task 2 着手時点でも継続しており（origin/claude/issue-121-impl-issue を merge した後も解消されない既存事象。merge 元の develop 経由で持ち込まれた事象）、本 task の変更とは無関係。`internal/repository` パッケージ全体での `go vet` / `go test` は当該既存事象でビルド失敗するが、本 task の新規ファイル単体は `go build ./internal/repository/...` で正常コンパイルすることを確認済。

### Task 3

- 採用方針: `ItemRepository` interface に `ListNewAcrossFeeds(ctx, userID, sinceTime, cursorPublishedAt, cursorItemID, limit) ([]CrossFeedItem, error)` を追加し、`postgres_item_repo.go` で `items × subscriptions × feeds × item_states (LEFT)` を 1 クエリで JOIN する実装を追加。cursor 有無で SQL を分岐し、cursor あり時のみ `(i.published_at, i.id) < ($3, $4::uuid)` のタプル比較を WHERE に含める（plain text の文字列比較を避けるため `::uuid` キャストを付与し、`SearchByUserAndKeyword` の既存パターンと整合）。新規 row 型 `CrossFeedItem` は `model.ItemWithState` を embed し `FeedTitle` / `FaviconData` / `FaviconMime` を併記する形で `interfaces.go` に追加した。
- 重要な判断: (1) cursor の有効性判定は `!cursorPublishedAt.IsZero() && cursorItemID != ""` の AND 条件とし、片方のみゼロ値で渡された場合は cursor なし扱い（先頭から取得）に倒すことで呼び出し側のバリデーション漏れを安全側に倒した。(2) integration test は `setupListDueTestDB` の既存ヘルパを流用するが、ヘルパ `insertCrossFeedTestItem` / `insertCrossFeedTestItemState` / `updateCrossFeedFeedFavicon` / `sortDescending` は同パッケージ内の他テスト群（`insertTestItem` / `insertStarredTestItem` 等）との命名衝突を避けるため、テストの目的を表す接頭辞 `CrossFeed` を付与した独立命名で定義した（task 2 で確立済の命名分離パターンに準拠）。テストは独立ファイル `postgres_item_repo_cross_feed_test.go` に分離し、既存 `postgres_item_repo_test.go` には触れていない。(3) `ItemRepository` interface への新メソッド追加に伴い `internal/item/service_test.go` の `mockItemRepoForService` と `internal/item/upsert_test.go` の `mockItemRepo` の interface 適合が失われたため、両モックに `ListNewAcrossFeeds` の最小スタブ（常に nil を返す）を追加した。これは task 3 の boundary（ItemRepository）の interface 拡張に追随する不可避な修正であり、`item` ドメインの挙動は変更していない。
- 残存課題: なし。task 1, 2 で言及されていた `postgres_item_repo_starred_test.go` での `insertTestItem` 重複事象は本 task 着手時点では **解消済み**（`grep -rn "func insertTestItem"` で `postgres_item_repo_search_test.go` の 1 箇所のみヒット）であり、`go vet ./...` / `go build ./...` / `go test -count=1 ./internal/repository/... ./internal/item/...` がいずれもクリーンに通る状態に復旧している。DB 結合テスト本体は `TEST_DATABASE_URL` が未設定の環境（CI / 本作業環境）ではスキップされるため、`go test -short` ではテスト本体が起動しない点は既存パターンと同じ。

### Task 4

- 採用方針: `internal/crossfeed/service.go` に `Service` / `NewItemsResult` / `CrossFeedItemSummary` と `ListNewItems` / `TouchLastSeen` を新規実装。sinceTime 決定は `resolveSinceTime` ヘルパに切り出して 3 段優先順位（overrideSince → stored lastSeen → now-24h fallback）を直線的に表現。cursor parse は `strings.LastIndex(s, ":")` で末尾の `:` を分割位置とし、RFC3339Nano が `:` を含む点を吸収（`item.parseItemCursor` と異なり複合カーソル対応のため独自実装）。favicon の data URL 構築は `internal/subscription/service.go` の `fmt.Sprintf("data:%s;base64,%s", mime, base64.StdEncoding.EncodeToString(data))` 方式と完全に整合。
- 重要な判断: (1) `CrossFeedItemSummary` は design.md の参考 Go 定義では `item.ItemSummary` を embed しつつ outer に `FeedID` を再宣言していたが、`item.ItemSummary` 自体が既に `FeedID` を持つため二重宣言となりコンパイル不可。embed 側のみで `FeedID` を運ぶ形に修正し、outer は `FeedTitle` / `FeedFaviconURL` のみとした（design.md の意図は満たしつつ Go 言語仕様に適合）。本判断は impl-notes 領分のため design.md には反映していない。(2) `nowFn func() time.Time` を Service の内部 hook として導入し、テストで固定時刻（fallback 24h 検証 / TouchLastSeen の now 引数検証）を注入可能にした。production 経路では nil のまま `time.Now()` が使われ既存挙動と等価。(3) `parseCrossFeedCursor` は `time.Parse(RFC3339Nano)` 失敗時に `time.Parse(RFC3339)` も試す fallback を入れて呼び出し側の正規化負担を緩めた（既存 `item.parseItemCursor` と同方針）。不正形式は `model.NewInvalidFilterError("invalid cursor: " + cursorStr)` で既存エラーコード `INVALID_FILTER` を再利用（新規コード追加なし）。(4) Repository モックは `internal/item/service_test.go` の `mockItemRepoForService` パターンに倣い、本テストで使う `ListNewAcrossFeeds` のみ関数差し替え可能にし、他 ItemRepository メソッドは no-op スタブで interface 適合のみ満たす形にした。
- 残存課題: なし。後続 task 5（CrossFeedHandler）が本 Service の `ListNewItems` / `TouchLastSeen` を直接呼ぶことを想定したシグネチャに整合。design.md「Service Interface」の `time.Now()` ベース TouchLastSeen と一致。

### Task 5

- 採用方針: `internal/handler/crossfeed_handler.go` に `CrossFeedServiceInterface` / `CrossFeedHandler` / `crossFeedItemResponse` / `crossFeedListResult` を新規実装。GET /api/items/cross-feed と PUT /api/users/me/cross-feed-last-seen を分離（PM 確認事項 / リトライ冪等性のため）。`CrossFeedServiceAdapter` は既存 `ItemServiceAdapterFromDomain` / `ItemSearchServiceAdapter` と同パターンで domain → handler DTO 変換。`internal/app/app.go` の `runServe` に DI 配線追加（`userCrossFeedViewRepo` → `crossfeed.NewService(itemRepo, userCrossFeedViewRepo)` → `handler.NewCrossFeedServiceAdapter` → `deps.CrossFeedService`）。
- 重要な判断: (1) `crossFeedItemResponse.FeedFaviconURL` は `*string` で **json タグから `omitempty` を外した**（`feed_favicon_url` フィールド自体は常に存在、未設定時は明示的に `null`）。これは Req 3.3 のクライアント側 fallback 判定（favicon 未設定なら `Rss` 代替アイコン表示）が「null vs 文字列」で判別する設計で、`omitempty` だとフィールド自体が消えクライアントが「未設定」を検知できなくなるため。既存 `itemSearchHitResponse.FaviconURL` は `omitempty` 付きだが、設計者の意図（design.md「レスポンス DTO」節）が常時 nullable フィールドを意図しているため本機能は別方針を採用した。(2) router 登録は `/api/items/cross-feed` を `/api/items/{id}` より **前**に登録し、chi v5 トライ木の静的セグメント優先動作に明示的に依存する（既存 `/api/feeds/starred/items` と同じ保護パターン）。(3) `RouterDeps.CrossFeedService` 配線が無い deps（既存テストの大半）でも router 構築が失敗しないよう、`crossFeedHandler` を nil ガードで作成し当該ルート登録もスキップする（後方互換、router_full_test 等の既存テストが破壊されない）。(4) handler テストは個別 handler 経由（13 件）+ router 経由（2 件: 正常ルーティング / 401）の 2 層で構成。router 経由テストでは既存 `mockSessionFinderForRouter` / `mockFeedService` 等を流用する `minimalRouterDepsForCrossFeed` ヘルパを切り出し、CrossFeedService 以外の handler 群は他ルートに到達しない前提で zero-value mock を渡す（NewRouter 構築時の参照のみ満たせばよい）。(5) `since` query parameter のパースは `time.Parse(time.RFC3339, v)` を使用（RFC3339Nano も受け付ける後方互換挙動）。失敗時は `INVALID_REQUEST` カスタム APIError（既存 `model.NewInvalidFilterError` は cursor 専用と整理）。
- 残存課題: なし。後続 task 6〜9（Frontend）が本 handler の API 契約（`feed_favicon_url` が `null` 可能、`since_time` 常時返却、`limit` 既定 50 / 上限 200）に依存することを前提に DTO を設計済。

### Task 6

- 採用方針: `FeedFavicon` を `web/src/components/feed-favicon.tsx` に named export として挙動不変で抽出し、`feed-list.tsx` の内部 private function 定義を削除して新パスから import。`AppStateContext` に `viewMode: 'none' | 'feed' | 'cross-feed'`（初期 `'none'`）と `crossFeedSessionSince: string | null`（初期 `null`、Req 4.7）を **追加**フィールドとして導入。`SELECT_ALL_NEW_ITEMS` action（viewMode='cross-feed' + selectedFeedId/expandedItemId reset + filter='all'、`crossFeedSessionSince` 保持）と `SET_CROSS_FEED_SESSION_SINCE` action（payload `sinceTime` で固定）を新設。既存 `SELECT_FEED` は viewMode='feed' に倒すロジックを追加（`crossFeedSessionSince` は保持、Req 4.7）。
- 重要な判断: (1) design.md / tasks.md は `AppState.viewMode: 'none' | 'feed' | 'cross-feed'` の単一 discriminator を前提としていたが、現行コードには #161 等で追加済の `selectedView: 'feed' | 'starred'`（starred 横断一覧用）が既に存在。両者を **並列の discriminator** として共存させる方針を採用した（既存 starred 機能との非リグレッション維持を優先）。`SELECT_ALL_NEW_ITEMS` は `selectedView='feed'` に倒して starred モードと排他にし、`SELECT_FEED` は両方の discriminator を整合させる。AppShell の分岐順序（task 9）では `isSearching > selectedView==='starred' > viewMode==='cross-feed' > 既存 ItemList` の優先で配線される想定。本判断は impl-notes 領分のため design.md / tasks.md は書き換えていない（後述「確認事項」に Reviewer 確認向けに記録）。(2) FeedFavicon は元コードを **完全に挙動不変**で切り出し、`useState` / props 型 / className / data-testid / `<img onError>` → fallback ロジックも変更なし。`lucide-react` の `Rss` import を新ファイル側に移し、`feed-list.tsx` 側からは `Rss` の import を削除した（dead import 除去）。(3) reducer test は既存「初期状態」テストに `viewMode='none'` / `crossFeedSessionSince=null` のアサーションを追加し、新規 4 テストで (a) SELECT_ALL_NEW_ITEMS の遷移、(b) SELECT_FEED で viewMode='feed' になること、(c) 横断 ⇄ 個別の往復で `crossFeedSessionSince` 保持、(d) SET_CROSS_FEED_SESSION_SINCE で固定、を網羅した（_Requirements: 1.2, 1.3, 4.7_）。FeedFavicon test は (a) faviconURL あり → `<img>`、(b) null → fallback、(c) 空文字 → fallback（境界値）、(d) onError 発火 → fallback 切替、の 4 ケース。
- 残存課題: (a) 後続 task 7 で FeedList props に `viewMode` / `onSelectAllNewItems` 追加が必要。本 task では `viewMode` を AppState に追加したのみで FeedList の signature は変更していない。(b) 後続 task 9 で AppShell の右ペイン分岐に `viewMode==='cross-feed'` 用の `<CrossFeedItemList />` 描画を追加する際、既存 `selectedView==='starred'` / `isSearching` との優先順位を明示的に決める必要がある（上記重要な判断 (1) の通り、本 impl-notes で示唆した順序を尊重するか task 9 着手時点で再確認）。

### Task 7

- 採用方針: `FeedList` props に `viewMode: ViewMode` と `onSelectAllNewItems: () => void` を **省略不可の必須 prop** として追加（design.md / tasks.md の指定通り）。「すべての新着記事」仮想エントリは既存 `feeds.map` の **直前**で常設描画し、購読 0 件でも表示する（Req 1.1）。`viewMode === 'cross-feed'` のとき既存個別フィード行と同じ `bg-accent text-accent-foreground font-medium` を付与、`data-testid="all-new-items-entry"` / `aria-current="page"` を選択中時のみ付与。favicon 領域は task 6 で抽出済の `<FeedFavicon feedId="__all__" faviconURL={null} feedTitle="すべての新着記事" />` を使用し代替 Rss アイコンを描画。「フィードが登録されていません」メッセージは仮想エントリより後ろの位置に配置し、購読配列が空のときのみ表示する（仮想エントリ自体は常時表示）。
- 重要な判断: (1) `viewMode === 'cross-feed'` のとき個別フィード行の `data-selected` は selectedFeedId 一致時でも **false** 扱いに倒した（横断一覧表示中は左ペインの「選択中」表示が仮想エントリに排他的に集中する設計、Req 1.4 の視覚的選択中表示の単一性確保）。これにより crossFeedSessionSince 保持のために `state.selectedFeedId` を意図的に temporarily 残す行き来パターン（task 9 で `SELECT_FEED` 経由）でも左ペインの選択中表示は破綻しない。(2) `app-shell.tsx` の `FeedList` 呼び出しは props 必須化により build エラーになるため、task 9 着手前の暫定として **zero-value**（`viewMode="none"` / `onSelectAllNewItems={() => {}}`）を渡す最小修正のみ加えた。task 9 で `state.viewMode` と `dispatch({ type: 'SELECT_ALL_NEW_ITEMS' })` への配線に差し替える前提で、本 task 段階では仮想エントリは UI 上クリック可能だが何も起きない状態となる（ロジック変更なし）。(3) `ViewMode` 型は task 6 で `web/src/contexts/app-state.tsx` から named export 済のものを import し、既存 export を変更しない安全側を採用。(4) 既存 17 件のテスト call site にも必須 props 補填（viewMode='none' / onSelectAllNewItems=() => {}）を行い、既存テストの挙動は変更なし。新規追加 4 件 + 仮想エントリと selectedFeedId の排他性確認 1 件の計 5 件を Issue #121 task 7 ブロックとしてセクション末尾に追記。
- 残存課題: (a) task 9 で `app-shell.tsx` 側を `viewMode={state.viewMode}` / `onSelectAllNewItems={() => dispatch({ type: 'SELECT_ALL_NEW_ITEMS' })}` に置き換える必要がある（本 task の暫定 zero-value を撤去）。(b) NFR 3.1（キーボード操作で個別フィードと同等に操作可能）について、仮想エントリは button 要素として実装したため Tab 移動 / Enter 押下で個別フィードと同等に発火する（既存個別フィード行も button のため挙動は揃う）。明示的なテストは追加していないが button 要素であることが要件を満たす根拠。

### Task 8

- 採用方針: `web/src/types/crossfeed.ts` に `CrossFeedItem` / `CrossFeedListResponse` を新規定義。`feed_favicon_url` は **`string | null`**（Task 5 で確立した「常時存在・null 可能」契約と整合、`omitempty` 不使用）。`web/src/hooks/use-cross-feed-items.ts` に `useCrossFeedItems()` と `useTouchCrossFeedLastSeen()` を新設。`useCrossFeedItems()` は `useAppState()` から `crossFeedSessionSince` を読み出し、queryKey に `crossFeedSessionSince ?? 'initial'` を含めて baseline 固定前後で別キャッシュとして扱う（Req 4.7）。URL は `URLSearchParams` ではなく **tasks.md 指定どおりの文字列連結方式** で構築（`?limit=50` を常に先頭に置き、`pageParam` 非空時に `&cursor=`、`crossFeedSessionSince` 非 null 時に `&since=` を `encodeURIComponent` で付与）。`staleTime: 0` で毎マウント時に必ず最新を取得。`useTouchCrossFeedLastSeen()` は `apiClient.put('/api/users/me/cross-feed-last-seen')` を呼ぶだけの軽量 mutation。`use-item-state.ts` の `useMarkAsRead` (`onSuccess`) と `useToggleStar` (`onSettled`) の双方に `queryClient.invalidateQueries({ queryKey: ['cross-feed-items'] })` を追加（Req 5.3）。
- 重要な判断: (1) `useCrossFeedItems` のテストで `useAppState` を直接 mock することで AppStateProvider のセットアップを回避し、`crossFeedSessionSince` の値別挙動（null / 非 null / 値変化時の refetch）を最小コードで検証可能にした。同パターンは既存 `use-feeds.test.tsx` / `use-items.test.tsx` の query mock pattern と整合する（fetch グローバル mock のみ + Provider 不要）。(2) URL 組み立ては tasks.md が `URLSearchParams` ではなく `'?limit=50' + (pageParam ? '&cursor=' + ... : '')` 形式を明示指定していたためそのまま採用。`URLSearchParams` を使う既存 `useItems` / `useStarredItems` と書き方が異なるが、tasks.md 指定優先（仕様逸脱を避けるため）。テストでも `encodeURIComponent(since)` の出力を直接 assertion する形にし、エンコーダ差異を吸収。(3) `useMarkAsRead` 側は既存の `onSuccess` ハンドラに 1 行追加、`useToggleStar` 側は既存の `onSettled` に 1 行追加するのみで、楽観的更新 (`onMutate` / `onError`) ロジックには触れていない（既存挙動の温存）。既存 `use-item-state.test.tsx` (3 件) は invalidate 対象 queryKey まで assertion していないため修正不要で全件 pass。(4) test 件数は要求 4 件以上に対し 4 件（hook test 3 件 + mutation test 1 件）。境界値・異常系として「since 値変化時に queryKey 別となり refetch 発火」のケースをカバーし、Req 4.7 の baseline 固定の動作保証を担保。
- 残存課題: なし。後続 task 9（`CrossFeedItemList` / AppShell 配線）が本 hook の戻り値型と `useTouchCrossFeedLastSeen().mutate()` 呼び出し pattern に依存する想定で signature を確定済み。`['cross-feed-items']` invalidation を `useMarkAsRead` / `useToggleStar` 双方に通したため、task 9 で記事行から既読化・スター操作した直後に横断一覧も自動 refetch される（手動 invalidation コードを task 9 側で追加する必要なし）。

### Task 9

- 採用方針: `web/src/components/cross-feed-item-list.tsx` を新規作成し、`useCrossFeedItems` で無限スクロール（IntersectionObserver、既存 `item-list.tsx` / `starred-item-list.tsx` と同 pattern）。各記事行は既存 `ItemRow` を再利用し、その直下に `FeedFavicon` + `feed_title` の badge を `flex items-center gap-1.5 text-xs text-muted-foreground` で併記する形を採用（`starred-item-list.tsx` の feed_title 表示パターンを横展開、Req 3.1 / 3.2 / 3.4）。空状態は専用 `cross-feed-item-list-empty` testid + 「新着記事はありません」（Req 4.6）。マウント時 `useEffect` で `crossFeedSessionSince === null` かつ初回 page 受信時のみ `hasInitializedSessionRef` で 1 回だけ (1) `queryClient.setQueryData(['cross-feed-items', sinceTime], data)` で新 queryKey にキャッシュ移送 → (2) `SET_CROSS_FEED_SESSION_SINCE` dispatch → (3) `useTouchCrossFeedLastSeen().mutate()` を実行（Req 4.3 / 4.7）。AppShell は分岐優先順位 `isSearching > selectedView==='starred' > viewMode==='cross-feed' > 既存 ItemList` を採用（impl-notes Task 6 確認事項 / 既存 starred モードとの排他維持）し、`FeedList` に `viewMode={state.viewMode}` と `onSelectAllNewItems={() => dispatch({ type: 'SELECT_ALL_NEW_ITEMS' })}` を配線して task 7 の暫定 zero-value を撤去した。
- 重要な判断: (1) **キャッシュ移送の必須化**: design.md「Component Interface」「useCrossFeedItems」節では `queryClient.setQueryData(['cross-feed-items', sinceTime], data)` を「必須ではない最適化」と位置付けていたが、当該移送を入れないと `SET_CROSS_FEED_SESSION_SINCE` dispatch によって `useCrossFeedItems` の queryKey が `['cross-feed-items', 'initial']` → `['cross-feed-items', sinceTime]` に切替わるタイミングで一瞬 `isLoading=true` に戻り、テスト上も連続的な data 観測が困難になることが判明したため、本実装では **常に移送を行う方針**を採用した（design の最適化指針を実体としては必須化したが、UX 向上の方向性は一致）。本判断は impl-notes 領分のため design.md は書き換えていない。(2) **touch mutation の参照は ref 経由**: `useEffect` の依存配列に `touchLastSeen`（mutation result オブジェクト全体）を含めると mutation 進行中の状態変化で再実行されるリスクがあるため、`touchMutateRef = useRef(touchLastSeen.mutate)` で mutate 関数のみを stable に保持し、依存配列からは除外した（不要な useEffect 再実行と PUT 重複呼び出しを排除）。(3) **`hasInitializedSessionRef` による発火制御**: `crossFeedSessionSince` が他経路（例: テスト内 StateProbe）で先に設定されていた場合、本 useEffect は `state.crossFeedSessionSince !== null` の early return で発火しない。さらにマウント直後の重複発火を防ぐため ref ガードも併用。テストでは「pre-existing since が設定済みのとき touch / dispatch が発火しない」ケースを直接検証した。(4) **`CrossFeedItem` → `ItemSummary` 射影**: 既存 `ItemRow` props は `ItemSummary` を要求するが、`CrossFeedItem` は `hatebu_fetched_at` を持たない（API レスポンスに含まれないため）。ローカル `toItemSummary` 関数で `hatebu_fetched_at: null` を補って射影する。`ItemRow` 内で当該フィールドは未参照のため挙動には影響しない。(5) **AppShell 分岐順序**: impl-notes Task 6 確認事項に従い `isSearching > selectedView==='starred' > viewMode==='cross-feed' > 既存 ItemList` の優先順序を採用。`SELECT_ALL_NEW_ITEMS` action 時に reducer 側で `selectedView='feed'` に倒しているため、cross-feed と starred は同時には共存しない（reducer 側で排他が成立）。(6) テスト件数は要求 6 件以上に対し 6 件、AppShell テストは要求 3 件以上に対し 3 件追加。`StateProbe` パターンを `starred-item-list.test.tsx` から流用し、AppStateContext の状態を test 外から観測する形で baseline 固定の検証を実現。
- 残存課題: なし。本 task で Issue #121 の全実装タスク（task 1〜9）が完了。次フェーズの Reviewer 確認で AC 1.1〜5.4 / NFR の全カバーを検証する想定。

## 確認事項

- task 1 で migration timestamp が design.md 指定（`20260528120000`）と既存 migration `20260528120000_add_item_search_indexes` で衝突したため、`20260528130000` に micro-adjust した。後続 task の本文中に同 timestamp を参照する箇所は無いため影響なし（design.md は書き換えていない）。
- `go vet ./...` 全体実行時に `internal/repository/postgres_item_repo_starred_test.go` で `insertTestItem redeclared in this block` の既存事象が出力されたが、本 task で触れていないファイル群であり、本 task の変更とは無関係（変更パッケージ `internal/model` / `internal/database` 単体での vet は pass）。後続 task または別 Issue での対処を要する可能性がある旨を Reviewer / PM に共有する。
- task 6: design.md / tasks.md は `AppState.viewMode` を単一の view discriminator として導入することを前提に書かれているが、当該 spec 起草以降に `selectedView: 'feed' | 'starred'`（横断スター記事一覧、Issue #161 等）が main に merge 済み。本 task では既存 `selectedView` を削除せず `viewMode` を並列の discriminator として導入し、`SELECT_ALL_NEW_ITEMS` 時に `selectedView` を `'feed'` に倒すことで starred モードと排他にする方針を採用した（既存 starred 機能の非リグレッション維持優先）。本判断は spec 範囲を逸脱しないが、Reviewer / PM は (a) `selectedView` を `'feed' | 'starred' | 'cross-feed'` に拡張して `viewMode` を導入しない案、(b) 本 task 採用の並列導入案、のどちらが望ましいかを確認したい場合は task 9 着手前に方針を共有してほしい。
- task 7: `FeedList` props を必須化したことに伴い、`web/src/components/app-shell.tsx` の `FeedList` 呼び出しでも `viewMode` / `onSelectAllNewItems` を渡す必要が生じた。task 7 の boundary は `FeedList` のみなので、ロジック変更を避けるため **zero-value（`viewMode="none"` / `onSelectAllNewItems={() => {}}`）を渡す最小修正**のみを暫定として加えた（実際の挙動：仮想エントリはクリック可能だが何も起きない / 選択中スタイルも付かない）。task 9 で `viewMode={state.viewMode}` / `onSelectAllNewItems={() => dispatch({ type: 'SELECT_ALL_NEW_ITEMS' })}` に差し替える前提。Reviewer は本暫定が task 9 で確実に解消されることを task 9 PR 確認時にチェックしてほしい。
